services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - SMARTPARK_TZ=Asia/Bangkok
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE=http://localhost:8080
    depends_on:
      - backend
